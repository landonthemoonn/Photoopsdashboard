
  # Create Design Prototype

  This is a code bundle for Create Design Prototype. The original project is available at https://www.figma.com/design/Zrx2tFjNW4osHf9M720wrM/Create-Design-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  